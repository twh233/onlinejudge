#include <iostream>
#include <cstdio>
using namespace std;
#define Max 10000
int main() {
	int a, b;
	while (cin >> a >> b) {
		if (a == 0 && b == 0)break;
		cout << a + b << endl;
	}
	while (scanf("%d%d",&a,&b)!=EOF) {
		cout << a - b << endl;
	}
	return 0;
}